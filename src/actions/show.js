const GET_EPISODES = "GET_EPISODES";
const GET_SHOW = "GET_SHOW";

const getEpisodes = (id) => {
  return {
    type: GET_EPISODES,
    show,
  };
};

const getShow = (id) => {
  return {
    type: GET_SHOW,
    show,
  };
};